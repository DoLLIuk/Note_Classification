<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Используем тему Material Components, так как в проекте используются компоненты Material Design -->
    <style name="Theme.MyApplication" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="mainBackgroundColor">@color/main_background_light</item>
        <item name="noteBackgroundColor">@android:color/white</item>
        <item name="toolbarBackgroundColor">@color/teal_200</item>
        <item name="fabBackgroundColor">@color/teal_200</item>
        <item name="noteTextColor">@android:color/black</item>
        <item name="iconTint">@color/icon_tint_color</item>
    </style>
</resources>
